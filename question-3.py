# Task (Tuples)
# Given there is n integer, perform the following actions:
#   - Take the input for n, the number of elements to be added in the tuple
#   - Add the elements in the tuple, no need to input the specific elements
#   - Use the built-in function hash()
#   - Print the tuple and the hash of the tuple

#Input Format => n is the number of elements that will be in the tuple

######code below this line######



######do not change these lines, only add functionalites if needed######
if __name__ == '__main__':
    numElements = int(input("Enter number of elements: "))